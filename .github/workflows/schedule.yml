name: Update gist with WakaTime stats
on:
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Update gist
        uses: matchai/waka-box@master
        env:
          GH_TOKEN: ${{ gist }}
          GIST_ID: a47cb86b13f21a015fee2177ba820c1a
          WAKATIME_API_KEY: ${{ 402d781a-5cde-413e-8cb2-3aece4b182de }}
